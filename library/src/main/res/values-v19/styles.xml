<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App theme -->
    <style name="AppTheme" parent="@style/BaseAppTheme">
        <!--4.4以上设置状态栏透明, 透明的原理是在Z轴方向, 将状态栏设置到布局之上，所以你会发现布局陷入到状态栏里面了。
        为了解决这个问题，需要将"垂直方向顶部"布局的android:fitsSystemWindows属性设为true,
        原理就是给这个布局加一个paddingTop值为状态栏高度的属性(4.4以下这个paddingTop的值为0, 所以兼容没有问题)-->
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>
